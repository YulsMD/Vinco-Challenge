const initialState = {
    champs: [],
    allChamps: [],
    details: []
}

const rootReducer = (state = initialState, action) =>{
  /* switch(action.type){
    default: state
  } */
}

export default rootReducer